spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zszg?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password: g54571571
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: true
    properties:
      hibernate.format_sql: true
      hibernate.dialect: org.hibernate.dialect.MySQL8Dialect
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 100MB
  jackson:
    serialization:
      fail-on-empty-beans: false
  # Redis配置
  data:
    redis:
      host: localhost
      port: 6379
      password:  # 如果Redis没有密码,留空
      database: 0
      timeout: 3000ms
      lettuce:
        pool:
          max-active: 20   # 最大连接数
          max-idle: 10     # 最大空闲连接
          min-idle: 5      # 最小空闲连接
          max-wait: 2000ms # 最大等待时间

server:
  port: 8080

app:
  jwt:
    secret: "change-this-secret-to-a-long-random-string"
    expiration-ms: 31536000000 # 365天 = 1年！再也不用担心过期了！
  cors:
    allowed-origins: "http://localhost:5173,http://localhost:5174"
  upload:
    path: uploads
  glm:
    api-key: "3f3508c9bcbe476db696356fb8ac6345.n2bT5A7uqHiNDd3l"
    api-url: "https://open.bigmodel.cn/api/paas/v4/chat/completions"
  baidu:
    ocr:
      # 百度OCR配置 - 请到 https://console.bce.baidu.com/ai/#/ai/ocr/overview/index 申请
      api-key: "YOUR_BAIDU_OCR_API_KEY"
      secret-key: "YOUR_BAIDU_OCR_SECRET_KEY"
    voice:
      # 百度语音配置 - 请到 https://console.bce.baidu.com/ai/#/ai/speech/overview/index 申请
      app-id: "YOUR_BAIDU_VOICE_APP_ID"
      api-key: "YOUR_BAIDU_VOICE_API_KEY"
      secret-key: "YOUR_BAIDU_VOICE_SECRET_KEY"

