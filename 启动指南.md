# 知错就改 - 启动指南

## 🚀 快速启动（推荐）

### 方法一：一键启动所有服务
双击运行项目根目录下的 `start-all.bat` 文件，它会自动：
1. 检查MySQL服务
2. 启动后端（8080端口）
3. 启动前端（5173端口）

### 方法二：只启动后端
双击运行 `start-backend.bat` 文件

## 📋 手动启动步骤

### 前提条件
1. ✅ 已安装 Java 17+
2. ✅ 已安装 Maven 3.6+
3. ✅ 已安装 Node.js 16+
4. ✅ 已安装 MySQL 8.0+
5. ✅ MySQL服务正在运行

### 步骤1：检查MySQL
```bash
# 检查MySQL服务状态
sc query MySQL80

# 如果未运行，启动MySQL服务
net start MySQL80
```

### 步骤2：初始化数据库
```bash
# 登录MySQL
mysql -u root -p

# 创建数据库
CREATE DATABASE IF NOT EXISTS zszg CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 退出
exit
```

### 步骤3：启动后端
```bash
# 打开新的命令行窗口
cd backend/zszg-backend

# 启动Spring Boot应用
mvn spring-boot:run

# 等待看到类似这样的输出：
# Started ZszgBackendApplication in X.XXX seconds
```

### 步骤4：启动前端
```bash
# 打开另一个新的命令行窗口
cd frontend/zszg-frontend

# 如果是首次启动，先安装依赖
npm install

# 启动开发服务器
npm run dev

# 访问 http://localhost:5173
```

## ⚠️ 常见问题

### 问题1：后端启动失败 - 端口被占用
```bash
# 查找占用8080端口的进程
netstat -ano | findstr "8080"

# 结束该进程（PID是上一步查到的进程ID）
taskkill /PID <进程ID> /F
```

### 问题2：数据库连接失败
检查 `backend/zszg-backend/src/main/resources/application.yml` 中的配置：
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zszg?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password: 你的MySQL密码
```

### 问题3：前端页面空白
1. 打开浏览器开发者工具（F12）
2. 查看Console标签是否有错误
3. 查看Network标签，确认API请求状态
4. 确保后端已经成功启动（检查 http://localhost:8080/api/auth/login 是否可访问）

### 问题4：Maven下载依赖慢
配置Maven使用阿里云镜像：
编辑 `~/.m2/settings.xml`（如果没有就创建）：
```xml
<mirrors>
  <mirror>
    <id>aliyun</id>
    <mirrorOf>central</mirrorOf>
    <name>Aliyun Maven</name>
    <url>https://maven.aliyun.com/repository/public</url>
  </mirror>
</mirrors>
```

## 🔍 验证启动成功

### 后端验证
访问: http://localhost:8080/api/auth/login
- 应该返回类似: `{"success":false,"message":"..."}`

### 前端验证
访问: http://localhost:5173
- 应该看到登录页面

## 📦 默认账户

系统会在首次启动时创建默认账户（查看 `init-data.sql`）：

**管理员账户**
- 用户名：admin
- 密码：admin123

**教师账户**
- 用户名：teacher1
- 密码：teacher123

**学生账户**
- 用户名：student1
- 密码：student123

## 🛑 停止服务

### 停止后端
在后端运行窗口按 `Ctrl + C`

### 停止前端
在前端运行窗口按 `Ctrl + C`

## 📝 开发模式

### 后端热重载
后端使用Spring Boot DevTools，修改代码后会自动重启

### 前端热更新
前端使用Vite，修改代码后浏览器会自动刷新

## 🔧 构建生产版本

### 后端打包
```bash
cd backend/zszg-backend
mvn clean package -DskipTests
# 生成的jar文件在 target/zszg-backend-0.0.1-SNAPSHOT.jar
```

### 前端打包
```bash
cd frontend/zszg-frontend
npm run build
# 生成的文件在 dist/ 目录
```

## 📞 需要帮助？

如果遇到问题：
1. 检查MySQL是否正在运行
2. 检查端口8080和5173是否被占用
3. 查看后端控制台日志
4. 查看浏览器开发者工具Console
5. 确认数据库配置正确


